# DS5110 HW1 - Messy Analysis & Coffee Sales Data

## Run
python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && python src/assignment.py


## Output
Prints average daily revenue ($158.25) and group means; saves output/revenue_by_product.png.

## AI use
Used Claude as a coding assistant while writing the pipeline, mainly for implementation details and git/environment setup
