# DS5110 HW1 - Coffee Sales Pipeline

## Run (one command, Python 3.11)
python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && python src/analysis.py

## Output
Printed results in the terminal; chart saved to output/revenue_by_product.png

## AI use
Used Claude (Anthropic) to identify reproducibility issues in the original notebook, draft the script, and set up the environment and git workflow.
